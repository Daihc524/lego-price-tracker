name: LEGO Price Tracker

on:
  schedule:
    - cron: '0 9 * * *'   # 每天早上 9 点抓价格
  workflow_dispatch:       # 支持手动触发

jobs:
  price-check:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout
      - name: Checkout repository
        uses: actions/checkout@v3
